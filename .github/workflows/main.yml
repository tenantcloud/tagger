---
name: Enlarge version
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
        with:
          fetch-depth: '0'
          global: true
          name: 'Oleh Debretseni'
          email: 'o.debretseni@tenantcloud.com'
          actor: 'odebretseni'
          token: '${{ secrets.GL_PAT_WEB_CENTRAL }}'
      - name: Enlarge version and push tag
        uses: tenantcloud/tagger@main
